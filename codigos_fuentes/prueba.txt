#include <stdio.h>
@
int main() {
    int a = 10;
    int b = 20;
    if (a < b) {
        printf("a es menor que b\n");
    } else {
        printf("a no es menor que b\n");
    }
    return 0;
}
